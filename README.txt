Mother Duck Kitchen — Quick Start

Serve the folder (required for ES module imports):
- Python:  python -m http.server 8080  -> http://localhost:8080/login.html
- VS Code Live Server: Right‑click login.html → Open with Live Server
- Node: npx http-server . -p 8080

Open login.html first. After sign-in you land on index.html.
